# How to build

```bash
$ cd path/to/project
$ yarn install
$ cd ios
$ pod install
$ open .
``` 

Then open workspace file in Xcode and go to `AppDelegate.m`. Enter your Google Maps API key there.
Build and run.